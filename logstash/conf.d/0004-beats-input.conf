# Author: Zachary Burnham (@zmbf0r3ns1cs)
# Comment out SSL options if not using

input {
  beats {
    port => 5044
    ssl => true
    ssl_certificate => "<pathToCert>"
    ssl_key => "<pathToKey>"
  }
}
